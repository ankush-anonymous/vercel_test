const mongoose = require("mongoose");
const bcrypt = require("bcryptjs");
const jwt = require("jsonwebtoken");

const {{camelCase name}}Schema = new mongoose.Schema({

})

module.exports = mongoose.model(" ", {{camelCase name}}Schema);